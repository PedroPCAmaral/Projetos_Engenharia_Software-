#include <stdio.h>

int main() {
    float base, altura, area;
    printf("informe a base do retangulo: ");
    scanf("%f", &base);
    printf("informe a altura do retangulo: ");
    scanf("%f", &altura);
    area = base * altura;
    printf("area do retângulo e: %.2f\n", area);
}
	